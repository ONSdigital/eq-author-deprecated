import {
  selectAddSurveyModal,
} from '../selectors'
import { fromJS } from 'immutable'
import expect from 'expect'

const selector = selectAddSurveyModal()

describe('selectAddSurveyModal', () => {

})
